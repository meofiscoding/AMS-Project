@{
    ViewData["Title"] = "Dashboard";
}


<style>
    .classrooms {
        display: flex;
        flex-wrap: wrap;
        margin: -15px;
    }

    .classroom-card {
        flex: 0 0 300px;
        margin: 15px;
        border: 1px solid #ddd;
        border-radius: 5px;
        overflow: hidden;
    }

    .classroom-card-header {
        display: flex;
        align-items: center;
        padding: 20px;
        background-color: #f9fafb;
    }

    .classroom-card-icon {
        font-size: 32px;
        color: #007bff;
        margin-right: 20px;
    }

    .classroom-card-title h2 {
        margin: 0;
        font-size: 24px;
    }

    .classroom-card-title p {
        margin: 0;
        font-size: 16px;
        color: #999;
    }

    .classroom-card-body {
        padding: 20px;
    }

    .btn {
        display: inline-block;
        font-weight: 400;
        color: #fff;
        text-align: center;
        vertical-align: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
        background-color: #007bff;
        border: 1px solid #007bff;
        border-radius: 0.25rem;
        padding: 0.375rem 0.75rem;
        font-size: 1rem;
        line-height: 1.5;
        transition: color 0.15s ease-in-out, background-color 0.15s ease-in-out, border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
    }

        .btn:hover {
            background-color: #0069d9;
            border-color: #0062cc;
        }

</style>

<div class="classrooms">
    @*  @if (Model.Any())
    {
    @foreach (var classroom in Model)
    {
    <div class="classroom-card">
    <div class="classroom-card-header">
    <div class="classroom-card-icon"><i class="fa fa-chalkboard"></i></div>
    <div class="classroom-card-title">
    <h2>@classroom.Name</h2>
    <p>Teacher: @classroom.TeacherName</p>
    </div>
    </div>
    <div class="classroom-card-body">
    <p>@classroom.Description</p>
    @if (User.IsInRole("Teacher"))
    {
    <a href="@Url.Action("Edit", "Classroom", new { id = classroom.Id })" class="btn btn-primary">Edit Class</a>
    }
    else if (User.IsInRole("Student"))
    {
    <form method="post" action="@Url.Action("JoinClassroom", "Classroom")">
    <input type="hidden" name="classroomId" value="@classroom.Id" />
    <div class="input-group">
    <input type="text" name="joinCode" placeholder="Enter join code" class="form-control" />
    <div class="input-group-append">
    <button type="submit" class="btn btn-primary">Join Class</button>
    </div>
    </div>
    </form>
    }
    </div>
    </div>
    }
    }
    else
    {
    @if (User.IsInRole("Teacher"))
    {
    <div class="no-classrooms">
    <p>You have not created any classes yet.</p>
    <a href="@Url.Action("CreateClassroom", "Classroom")" class="btn btn-primary">Create Class</a>
    </div>
    }
    else if (User.IsInRole("Student"))
    {
    <div class="no-classrooms">
    <p>You are not enrolled in any classes.</p>
    </div>
    }
    }*@

  <div class="classroom-card">
      <div class="classroom-card-header">
        <div class="classroom-card-icon"><i class="fa fa-chalkboard"></i></div>
        <div class="classroom-card-title">
          <h2>classroom.Name</h2>
          <p>Teacher: classroom.TeacherName</p>
        </div>
      </div>
      <div class="classroom-card-body">
        <p>classroom.Description</p>
        <a href="#" class="btn btn-primary">Join Class</a>
      </div>
    </div>
</div>